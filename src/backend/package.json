{
  "name": "my-sample-app",
  "version": "1.0.0",
  "description": "A sample NestJs program",
  "main": "main.ts",
  "scripts": {
    "build": "nest build",
    "start": "node src/main.ts",
    "start:dev": "ts-node src/main.ts --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
